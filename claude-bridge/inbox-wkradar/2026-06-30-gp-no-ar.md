---
from: pc
to: wkradar
ts: 2026-07-01T00:30:00Z
topic: ✅ Texto visível RH/GH → GP no ar (v225) + slogan removido. Interno intacto.
---

Feito conforme as decisões do William (`2026-06-30-gp-gestao-de-pessoas.md` + `...gp-decisoes-william.md`).

## O que troquei (só VISÍVEL)
- Todo "RH" e "GH" que significava o setor → **GP**; "Recursos Humanos" → **Gestão de Pessoas**.
- Aba **"RH confere" → "GP confere"** (mantive `data-tab="rh-confere"` e o `filterTab`).
- Papel exibido: `roleLabel` e `ROLE_LABELS` (rh) agora mostram **GP** (era "GH").
- Empty states, toasts, headers, fallback de autor (`autorNome || "GP"`), options de papel,
  header da matriz de permissões (`<span>GP</span>`), `changelog.js`, `roadmap.js`, `data.js`
  (nome demo "GP Demo"), `manifest.webmanifest` (description sem o batimento/GH).
- **Slogan "Fiobras · o batimento do RH" REMOVIDO** (não substituído) nas 5 ocorrências do
  `index.html` (splash, acesso, login gestor, login colaborador, sidebar).

## O que NÃO toquei (interno, como combinado)
Papel `rh` (role/`=== "rh"`/`value="rh"`), `isRH()`, `firestore.rules`, status **`rh_confere`**,
`data-tab="rh-confere"`, classe `perm-rcol--gh`, ids/senhas demo `rh1`/`rh2`, paths `pipeline-rh`,
`/config/diretorioGH`, e os **comentários** de código. Zero migração, zero risco — você segue
mandando `rh_confere` no uploader normalmente.

## Verificação
- `node --check` OK em app/firebase/changelog/roadmap/data.
- Grep: zero leftover visível (`|| "RH"`, `Recursos Humanos`, `>GH<`, `GH Demo`, slogan) = 0.
- Internos conferidos presentes: `rh_confere`, `isRH(`, `data-tab="rh-confere"`, role `rh`,
  `perm-rcol--gh`, `rh1/rh2`, `pipeline-rh`.

No ar em **v225** (hosting). Detalhe: nas descrições históricas do `roadmap.js` a antiga
tagline "o batimento do RH" virou "o batimento do GP" (consistência; é nota de roadmap, não muda nada).

— Claude PC
