---
from: wkradar
to: pc
ts: 2026-06-17T14:00:00Z
topic: v1.9.1 (tag Menor Aprendiz) — CÓDIGO PRONTO, só falta você deployar
---

O William me autorizou a implementar a tag de **menor aprendiz** direto no app (mesmo padrão do diretor). **Já está no código e pushado** — só não consigo deployar porque **não tem `firebase login` no servidor do pipeline**.

## Pra você (PC): só deployar
```
git pull origin main
firebase deploy --only hosting
```
Depois move `docs/wkradar-missions/pending/2026-06-17-tag-menor-aprendiz.md` → `done/`.

## O que eu mexi (commit `c8741fc`, v1.9.1)
- `public/app.js`: badge `badge--neutral` "Menor Aprendiz" na lista; selo no perfil; opção "Aprendizes" no `#func-status-filter`; `f.aprendiz !== true` somado nos filtros de **BH** (`renderBancoHoras`), **demografia** (`renderDemografiaWidget`) e **ranking** (`renderRankingTempoCasaWidget`) + no "operacional"; `CURRENT_VERSION = "1.9.1"`.
- `public/changelog.js`: entry 1.9.1 no topo (current).
- `public/sw.js`: `CACHE = "fiopulse-v158"`.
- `public/index.html`: `?v=157` → `?v=158` (6×).
- `node --check` passou nos 3 JS.

## Tratamento (= diretor)
Aprendiz: conta no **quadro + listagem + aniversário**; fora de **BH, demografia, ranking**. Flag `aprendiz` (boolean) já vem do Firestore (`funcionarios`, `pipeline-rh`, `bancoHoras`). Contagens: 95 ativos · ranking-operacional 82 · demografia 88.

Se algo no meu código não seguir teu padrão, fica à vontade pra ajustar antes de deployar. Valeu! — Claude WKRADAR
